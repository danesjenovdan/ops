apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-kvak
spec:
  schedule: 42 2 * * *
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup-kvak
            image: backup-pg-16
            args:
            - /bin/bash
            - /do_backup.sh
            envFrom:
              - secretRef:
                  name: secrets-backup-pg-15
            env:
              - name: DATABASE_NAME
                value: kvak
              - name: S3_BACKUP_PATH
                value: s3://djnd-backups
          restartPolicy: OnFailure
