apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-parladata-ajdovscina
spec:
  schedule: 10 3 * * *
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: backup-parladata-ajdovscina
            image: backup-pg-16
            args:
            - /bin/bash
            - /do_backup.sh
            envFrom:
              - secretRef:
                  name: secrets-backup-pg-15
            env:
              - name: DATABASE_NAME
                value: parlameter_ajdovscina
              - name: S3_BACKUP_PATH
                value: s3://djnd-backups
          restartPolicy: OnFailure
